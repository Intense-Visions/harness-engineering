---
---

perf-fleet — new `-fleet` family member (performance-budget/regression remediation sweep). Skill-authoring + generated-docs change only; no publishable package `src/` is touched, so this is an intentional no-release changeset.
