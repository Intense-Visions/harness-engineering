---
name: harness:api-craft
description: LLM-judgment critique of API design quality — the ceiling counterpart to rule-based API checks (OpenAPI-format and webhook-format compliance). Asks whether resources model the domain rather than the implementation, whether resource naming and URL structure are predictable (path vs query param), whether HTTP methods are honest, whether status codes are correct, whether error responses tell the consumer what to do, whether response shapes are predictable and consistent, whether collections paginate and filter consistently, whether mutations are idempotency-honest, and whether the API evolves without breaking consumers. Structural twin of harness-cli-ergonomics-craft.
argument-hint: '[--path <path>] [--files <files>] [--routes-dir <routes-dir>] [--spec-file <spec-file>] [--exclude-dirs <exclude-dirs>] [--max-files <max-files>]'
allowed-tools:
  - Bash
  - Read
  - Glob
  - Grep
---

<!-- Generated by harness generate-slash-commands. Do not edit. -->

<context>
Cognitive mode: constructive-architect
Type: rigid
</context>

<objective>
LLM-judgment critique of API design quality — the ceiling counterpart to rule-based API checks (OpenAPI-format and webhook-format compliance). Asks whether resources model the domain rather than the implementation, whether resource naming and URL structure are predictable (path vs query param), whether HTTP methods are honest, whether status codes are correct, whether error responses tell the consumer what to do, whether response shapes are predictable and consistent, whether collections paginate and filter consistently, whether mutations are idempotency-honest, and whether the API evolves without breaking consumers. Structural twin of harness-cli-ergonomics-craft.

Phases:

- discover: Discover OpenAPI/Swagger documents and route/handler definitions; classify each surface as openapi or route; exclude tests, barrels, helpers, and build dirs
- critique: LLM-rubric loop per (surface, rubric) filtered by surface kind — 9 seed rubrics
- report: Aggregate findings + rubric/exemplar catalog + cost telemetry + surface counts
  </objective>

<execution_context>
@agents/skills/claude-code/api-craft/SKILL.md
@agents/skills/claude-code/api-craft/skill.yaml
</execution_context>

<process>
1. Try: invoke mcp__harness__api_craft with skill: "api-craft"
2. If MCP unavailable: read SKILL.md and follow its workflow directly
3. Pass through any arguments provided by the user
</process>
