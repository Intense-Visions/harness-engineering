---
name: harness:docs-fleet
description: Autonomous documentation-drift remediation sweep — enumerate the doc-drift/undocumented backlog by composing the existing documentation-floor detectors, rank the areas by staleness value, confirm the batch once, fan out worktree-isolated subagents that each run the real per-area harness-docs-pipeline to convergence, independently verify each result by a clean doc re-scan and all-OS CI, and hand back a batch of scoped doc-fix PRs for one bulk human review. Never auto-merges.
argument-hint: '[--path <path>] [----concurrency <--concurrency>] [----report-only <--report-only>] [----dry-run <--dry-run>] [----safe-only <--safe-only>]'
allowed-tools:
  - Bash
  - Read
  - Glob
  - Grep
---

<!-- Generated by harness generate-slash-commands. Do not edit. -->

<context>
Cognitive mode: systematic-orchestrator
Type: rigid
</context>

<objective>
Autonomous documentation-drift remediation sweep — enumerate the doc-drift/undocumented backlog by composing the existing documentation-floor detectors, rank the areas by staleness value, confirm the batch once, fan out worktree-isolated subagents that each run the real per-area harness-docs-pipeline to convergence, independently verify each result by a clean doc re-scan and all-OS CI, and hand back a batch of scoped doc-fix PRs for one bulk human review. Never auto-merges.

Phases:

- select: Enumerate the documentation-drift/coverage backlog by composing detect-doc-drift, harness check-docs, harness-docs-pipeline (report-only) and a doc-churn pass into doc-fix areas, cross-check against merged/open PRs, classify safe vs probably-safe vs unsafe, and score by composite churn-gap x drift-density x coverage-gap
- confirm: Present the ranked doc-area batch in one round — already-fresh areas flagged for drop, fix classifications confirmed, unsafe areas flagged as will-park, proposed concurrency — for a single up-front human approval
- dispatch: Fan out worktree-isolated subagents, each running the real harness-docs-pipeline --fix scoped to one area, capped at the concurrency governor and consulting the per-leaf context budget; an area needing an unsafe documentation change parks and reports
- verify: Independently confirm each returned branch converged (opening drift/coverage findings resolved and a fresh detect-doc-drift + harness check-docs re-scan clean) and is CI-green across all target operating systems plus the project's required checks against a fresh base — never by subagent self-report
- report: Emit a one-row-per-area batch summary with findings-resolved counts, assumptions notes, and parked unsafe remediations for bulk human review, annotate already-fresh areas citing the resolving PR, never merge
  </objective>

<execution_context>
@agents/skills/claude-code/docs-fleet/SKILL.md
@agents/skills/claude-code/docs-fleet/skill.yaml
</execution_context>

<process>
1. Try: invoke mcp__harness__run_skill with skill: "docs-fleet"
2. If MCP unavailable: read SKILL.md and follow its workflow directly
3. Pass through any arguments provided by the user
</process>
