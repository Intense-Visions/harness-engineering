---
name: harness:graph-integrity
description: Check a knowledge graph for content that cannot be trusted — connectors that stamped a fresh sync timestamp while hard-failing, and extractor-derived nodes minted out of prose rather than code. Orchestrates harness graph integrity; never reimplements detection. Reports denominators, so a run that inspected nothing abstains instead of passing.
---

<!-- Generated by harness generate-slash-commands. Do not edit. -->

<context>
Cognitive mode: meticulous-verifier
Type: rigid
</context>

<objective>
Check a knowledge graph for content that cannot be trusted — connectors that stamped a fresh sync timestamp while hard-failing, and extractor-derived nodes minted out of prose rather than code. Orchestrates harness graph integrity; never reimplements detection. Reports denominators, so a run that inspected nothing abstains instead of passing.

Phases:

- inspect: Run harness graph integrity against the target project
- triage: Separate connector abstentions from extractor debris and name what is untrusted
- report: Surface findings with denominators and the remediation each class needs
  </objective>

<execution_context>
@agents/skills/cursor/harness-graph-integrity/SKILL.md
@agents/skills/cursor/harness-graph-integrity/skill.yaml
</execution_context>

<process>
1. Try: invoke mcp__harness__run_skill with skill: "harness-graph-integrity"
2. If MCP unavailable: read SKILL.md and follow its workflow directly
3. Pass through any arguments provided by the user
</process>
