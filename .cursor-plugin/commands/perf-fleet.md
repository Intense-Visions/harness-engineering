---
name: harness:perf-fleet
description: Autonomous performance-budget/regression remediation sweep — enumerate the perf-budget-violation and runtime-regression backlog by composing the existing perf detectors (check_performance, harness-perf, benchmark baselines), fold it into independent remediation targets, rank them by severity value, confirm the batch once, fan out worktree-isolated subagents that each run the real per-target measure-remediate-remeasure pipeline gated by a measured before/after, independently verify each result by a re-measurement that proves the budget is met plus updated baselines plus all-OS CI, and hand back a tiered batch of perf-fix PRs and filed redesign issues for one bulk human review. No measured before/after, no fix. Never auto-merges and never rebaselines a regression away.
---

<!-- Generated by harness generate-slash-commands. Do not edit. -->

<context>
Cognitive mode: systematic-orchestrator
Type: rigid
</context>

<objective>
Autonomous performance-budget/regression remediation sweep — enumerate the perf-budget-violation and runtime-regression backlog by composing the existing perf detectors (check_performance, harness-perf, benchmark baselines), fold it into independent remediation targets, rank them by severity value, confirm the batch once, fan out worktree-isolated subagents that each run the real per-target measure-remediate-remeasure pipeline gated by a measured before/after, independently verify each result by a re-measurement that proves the budget is met plus updated baselines plus all-OS CI, and hand back a tiered batch of perf-fix PRs and filed redesign issues for one bulk human review. No measured before/after, no fix. Never auto-merges and never rebaselines a regression away.

Phases:

- select: Enumerate the perf-budget-violation and runtime-regression backlog by composing check_performance, harness check-perf (structural + coupling), the benchmark regression detector against get_perf_baselines, and a churn pass into remediation targets, cross-check against merged/open PRs, classify safe vs risky, and score by composite tier-severity x critical-path x churn
- confirm: Present the ranked target batch in one round — already-optimized targets flagged for drop, safe/risky classifications confirmed, risky redesigns flagged as will-file, proposed concurrency — for a single up-front human approval
- dispatch: Fan out worktree-isolated subagents, each running the real per-target measure-remediate-remeasure pipeline (harness-perf establishes the before, harness-debugging or harness-refactoring drives the fix, harness-perf re-measures the after) capped at the concurrency governor and consulting the per-leaf context budget; a target needing a risky redesign files an issue with its measurement evidence rather than auto-applying
- verify: Independently confirm each returned branch cleared the measured before/after bar (the target opened with a measured violation and a re-measurement proves the budget is met), that baselines were updated via update_perf_baselines rather than hand-edited to mask the regression, and that CI is green across all target operating systems plus the project's required checks against a fresh base — never by subagent self-report
- report: Emit a one-row-per-target tiered batch summary — verified fix PRs with before/after numbers and assumptions notes, plus filed redesign issues carrying the measurement evidence — for bulk human review, annotate already-optimized targets citing the resolving PR, never merge
  </objective>

<execution_context>
@agents/skills/cursor/perf-fleet/SKILL.md
@agents/skills/cursor/perf-fleet/skill.yaml
</execution_context>

<process>
1. Try: invoke mcp__harness__run_skill with skill: "perf-fleet"
2. If MCP unavailable: read SKILL.md and follow its workflow directly
3. Pass through any arguments provided by the user
</process>
