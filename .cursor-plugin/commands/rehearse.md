---
name: harness:rehearse
description: Rehearse an agent against a deliberately-broken fixture and score how well it recovers. Picks a fixture from templates/rehearsal-fixtures/ (each plants one failure mode a real harness check catches — leaked secret, layer violation, dependency cycle, broken doc link), copies it into a scratch workspace, has the agent detect and repair the planted defect, assembles a structured recovery record, and runs `harness rehearse score` for a deterministic 0-100 score and pass/partial/fail tier across four dimensions (detected, correctCheck, fixed, noCollateral). Used to train personas before production trust and to regression-test the harness's own gates against known failure shapes.
---

<!-- Generated by harness generate-slash-commands. Do not edit. -->

<context>
Cognitive mode: constructive-architect
Type: rigid
</context>

<objective>
Rehearse an agent against a deliberately-broken fixture and score how well it recovers. Picks a fixture from templates/rehearsal-fixtures/ (each plants one failure mode a real harness check catches — leaked secret, layer violation, dependency cycle, broken doc link), copies it into a scratch workspace, has the agent detect and repair the planted defect, assembles a structured recovery record, and runs `harness rehearse score` for a deterministic 0-100 score and pass/partial/fail tier across four dimensions (detected, correctCheck, fixed, noCollateral). Used to train personas before production trust and to regression-test the harness's own gates against known failure shapes.

Phases:

- select: Run `harness rehearse list` and pick a fixture; read its manifest via `harness rehearse show <id>`
- stage: Copy the fixture directory into an isolated scratch workspace so the repo under test is untouched
- recover: In the scratch copy, detect the planted defect, reach for the fixture's expected harness check, and repair it without breaking siblings
- record: Assemble a structured recovery record (detected, identifiedFailureMode, checkCited, fixed, collateralDamage)
- score: Run `harness rehearse score --fixture <id> --recovery <record.json>`; report the 0-100 score, tier, and per-dimension breakdown
  </objective>

<execution_context>
@agents/skills/cursor/harness-rehearse/SKILL.md
@agents/skills/cursor/harness-rehearse/skill.yaml
</execution_context>

<process>
1. Try: invoke mcp__harness__run_skill with skill: "harness-rehearse"
2. If MCP unavailable: read SKILL.md and follow its workflow directly
3. Pass through any arguments provided by the user
</process>
