---
name: harness:uat-signoff
description: Human-judged acceptance sign-off skill — the terminal, human-authority stage of the change lifecycle under the `docs/changes/<slug>/` directory. Reads the change's proposal.md Success Criteria (plus plans and prior review/outcome-eval records) as the acceptance checklist, walks a human through user-acceptance testing one item at a time (ACCEPT / REJECT / CHANGES_REQUESTED), captures one overall decision plus the signer, writes `docs/changes/<slug>/signoff.md`, and records a single execution_outcome node via uat_signoff. The human is the authority — it never runs an LLM verdict, never derives ship authority, and never blocks. Advisory / record-only.
---

<!-- Generated by harness generate-slash-commands. Do not edit. -->

<context>
Cognitive mode: configuration-interviewer
Type: rigid
State: persistent (files: docs/changes/)
</context>

<objective>
Human-judged acceptance sign-off skill — the terminal, human-authority stage of the change lifecycle under the `docs/changes/<slug>/` directory. Reads the change's proposal.md Success Criteria (plus plans and prior review/outcome-eval records) as the acceptance checklist, walks a human through user-acceptance testing one item at a time (ACCEPT / REJECT / CHANGES_REQUESTED), captures one overall decision plus the signer, writes `docs/changes/<slug>/signoff.md`, and records a single execution_outcome node via uat_signoff. The human is the authority — it never runs an LLM verdict, never derives ship authority, and never blocks. Advisory / record-only.

Phases:

- resolve: Resolve the change slug and read docs/changes/<slug>/proposal.md
- interview: Present each acceptance item to the human one at a time (plain-text choice), capturing ACCEPT / REJECT / CHANGES_REQUESTED plus a note verbatim. Never infer a disposition.
- verdict: Ask the human for one overall decision (ACCEPTED / REJECTED / CHANGES_REQUESTED) and the signer identity; read back and confirm before recording.
- record: Write docs/changes/<slug>/signoff.md (accepted-vs-rejected + signer + timestamp) and persist one execution_outcome node via uat_signoff (source uat-signoff, result derived from the overall decision). Advisory / record-only — blocks nothing.
  </objective>

<execution_context>
@agents/skills/cursor/uat-signoff/SKILL.md
@agents/skills/cursor/uat-signoff/skill.yaml
</execution_context>

<process>
1. Try: invoke mcp__harness__uat_signoff with skill: "uat-signoff"
2. If MCP unavailable: read SKILL.md and follow its workflow directly
3. Pass through any arguments provided by the user
</process>
