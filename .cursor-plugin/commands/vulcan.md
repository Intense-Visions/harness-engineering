---
name: capillary:vulcan
description: Reads LLD (Low-Level Design) documents, feature specs, or SDD files from a docs/ folder in a Vulcan SDK project and generates fully functioning code following the Vulcan + Cap UI + Redux-Saga + Atomic Design patterns. The SA team provides these docs as either feature-wise .md files or a single LLD document describing routes, API contracts, UI elements, and error behavior. Use this skill whenever the user wants to implement features from an LLD or SDD in a Capillary Vulcan project, wants to scaffold pages/modules from design docs, or says things like "build from the LLD", "implement the spec", "create the module from docs", "read the design doc and build it", or "scaffold from the LLD". Also use when the user mentions building partner portals, loyalty modules, or any Vulcan microsite features from specification documents. Trigger this even if the user just says "implement" or "build" and there are LLD/SDD/spec .md files in the project's docs/ folder. Also trigger when plan files from a previous session exist in docs/plans/ — this means the user wants to continue with code generation.
---

<!-- Generated by harness generate-slash-commands. Do not edit. -->

<context>
Cognitive mode: meticulous-implementer
Type: flexible
</context>

<objective>
Reads LLD (Low-Level Design) documents, feature specs, or SDD files from a docs/ folder in a Vulcan SDK project and generates fully functioning code following the Vulcan + Cap UI + Redux-Saga + Atomic Design patterns. The SA team provides these docs as either feature-wise .md files or a single LLD document describing routes, API contracts, UI elements, and error behavior. Use this skill whenever the user wants to implement features from an LLD or SDD in a Capillary Vulcan project, wants to scaffold pages/modules from design docs, or says things like "build from the LLD", "implement the spec", "create the module from docs", "read the design doc and build it", or "scaffold from the LLD". Also use when the user mentions building partner portals, loyalty modules, or any Vulcan microsite features from specification documents. Trigger this even if the user just says "implement" or "build" and there are LLD/SDD/spec .md files in the project's docs/ folder. Also trigger when plan files from a previous session exist in docs/plans/ — this means the user wants to continue with code generation.

Phases:

- discover: Find and parse LLD/SDD docs in docs/; detect Session 2 shortcut if docs/plans/ exists
- plan: Analyze codebase, run gap analysis, write self-contained plan to docs/plans/<feature-slug>.md (optional)
- implement: Read plan file, generate code following Vulcan file structure per page (optional)
- verify: Start dev server (nvm use 14 && npm start), confirm compilation, capture lessons (optional)
  </objective>

<execution_context>
@../../.harness/skills/community/claude-code/capillary-vulcan/SKILL.md
@../../.harness/skills/community/claude-code/capillary-vulcan/skill.yaml
</execution_context>

<process>
1. Try: invoke mcp__harness__run_skill with skill: "capillary-vulcan"
2. If MCP unavailable: read SKILL.md and follow its workflow directly
3. Pass through any arguments provided by the user
</process>
