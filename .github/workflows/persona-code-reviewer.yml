# GENERATED FILE — do not edit by hand.
# Source persona: agents/personas/code-reviewer.yaml (outputs.ci-workflow: true)
# Regenerate:   pnpm generate:persona-workflows
# Drift guard:  pnpm generate:persona-workflows:check  (enforced in CI)
#
# Advisory (continue-on-error): honors the persona's declared triggers at the
# CLI-command tier without blocking the PR. The persona's LLM/agent-runtime
# steps (skill steps) are delivered via required-review.yml, not here. See
# docs/changes/honor-persona-triggers/proposal.md (#663).

name: Code Reviewer
on:
  pull_request:
    paths:
      - src/**
      - packages/**
  push:
    branches:
      - main
      - develop
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
jobs:
  enforce:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - run: node packages/cli/dist/bin/harness.js validate --severity error
      - run: node packages/cli/dist/bin/harness.js check-deps --severity error
      - run: node packages/cli/dist/bin/harness.js check-docs --severity error
      - run: node packages/cli/dist/bin/harness.js check-perf --severity error
