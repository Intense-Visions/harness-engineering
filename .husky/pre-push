export PATH="/opt/homebrew/bin:$PATH"

# Fast checks only — format/lint already run via pre-commit (lint-staged).
# Full harness ci check, docs:build, and format:check run in CI.
pnpm run typecheck
pnpm run test:ci
node scripts/coverage-ratchet.mjs
pnpm run generate-docs --check
