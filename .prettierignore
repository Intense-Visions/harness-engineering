node_modules
dist
build
coverage
.turbo
.next
out
.tmp-*
pnpm-lock.yaml
*.min.js
# Generated slash commands (YAML frontmatter must not be reformatted)
agents/commands/**

# Machine-managed roadmap: the deterministic serializer owns the format of the
# generated aggregate and the per-row shards (double-quoted YAML frontmatter is a
# deliberate safety choice). Prettier would re-pad tables / single-quote YAML,
# fighting the regenerator forever and tripping the aggregate-drift check.
docs/roadmap.md
docs/roadmap.d/**

# Generated tool & skill catalog: scripts/generate-tool-catalog.mjs owns the
# byte-exact format (deep-sorted JSON schemas). Prettier's embedded-JSON array
# formatting is non-idempotent around its print-width boundary, so it would
# fight the regenerate-and-gate check forever (see the script's header comment).
docs/reference/tool-catalog.md

# Generated/local harness state files
**/.harness/state.*
.harness/health-snapshot.json
.harness/skills-index.json
.harness/specialization-profiles.json
.harness/stack-profile.json
.harness/metrics/
.harness/security/
.harness/streams/
.harness/black-box/
.harness/sessions/
.harness/proposals/
**/.harness/graph/
**/.harness/arch/
**/fixtures/**/.harness/
**/.harness/arch/baselines.json

# Intentional syntax-error test fixtures
packages/core/tests/fixtures/typescript-samples/syntax-error.ts
packages/core/tests/fixtures/code-nav/syntax-error.ts

# Empty changeset no-release marker: its frontmatter must stay `---\n\n---`
# (blank line between delimiters) so scripts/check-changesets.mjs detects it as
# empty. Prettier would collapse it to `---\n---`, which the gate rejects.
.changeset/maint-arch-refactor-no-release.md
.changeset/downstream-papercuts-orchestrator-comment-no-release.md
.changeset/repo-shape-orchestrator-comment-no-release.md
.changeset/workflow-audit-orchestrator-comment-no-release.md
.changeset/stagedecision-jsdoc-comment-no-release.md
.changeset/codex-test-windows-skip-no-release.md
.changeset/dependabot-alert-cleanup-no-release.md

# Local orchestrator state backups (disposable)
**/.harness/state-backup-*/

# Solution-doc parser fixtures: the frontmatter IS the test input. Prettier
# normalises double-quoted YAML scalars to single quotes, which silently deletes
# the double-quote coverage in BusinessKnowledgeIngestor.solutions.test.ts — and
# would be free to rewrite crlf-pattern.md's line endings, the whole point of
# that fixture. Same reasoning as agents/commands/** above.
packages/graph/tests/fixtures/solutions/**

# Compiled-comprehension substrate: generated `_module.md` shards emitted by
# `harness comprehend` with deterministic double-quoted YAML frontmatter
# (quoteYamlScalar). Prettier would flip those to single quotes, colliding with
# lint-staged's stash/restore on bulk `comprehend --all` runs and leaving a few
# shards dribbling modified after every commit (see #1697). The shards are
# already deterministic, so keep prettier out of them entirely.
.harness/comprehension/**
