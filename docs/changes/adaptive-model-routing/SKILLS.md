# Recommended Skills: Adaptive Model Routing (AMR)

> Generated by `advise_skills` (758 skills scanned, 570ms), then curated. AMR is core routing infrastructure, not a skill-shaped feature — no **Apply**-tier matches surfaced. The references below are pattern skills worth consulting during implementation.

## Reference (load as context during implementation)

| Skill                         | When           | Why relevant to AMR                                                                               |
| ----------------------------- | -------------- | ------------------------------------------------------------------------------------------------- |
| `gof-chain-of-responsibility` | Architecture   | The resolution walk + fallback-chain / tier-escalation ladder is a chain-of-responsibility (0.58) |
| `ts-performance-patterns`     | Implementation | Cheap-first cascade — keep the router cost far below what it saves (0.58)                         |
| `ts-type-guards`              | Implementation | Discriminated `BackendDef` narrowing + `RoutingUseCase` variants (0.54)                           |
| `ts-testing-types`            | Testing        | Type-level tests for the new `RoutingRequest` / `RoutingPolicy` shapes (0.54)                     |
| `ts-zod-integration`          | Architecture   | `RoutingPolicy` / `BackendCapabilities` config validation via Zod (0.53)                          |
| `gof-factory-method`          | Architecture   | Registry construction extends the existing `createBackend` factory (0.51)                         |

## Consider

| Skill               | When    | Why                                                                                                         |
| ------------------- | ------- | ----------------------------------------------------------------------------------------------------------- |
| `test-tdd-workflow` | Testing | Pure fns (`deriveRequiredTier`, `selectCheapestQualifying`, `escalationFloor`) are ideal TDD targets (0.35) |

## Notes

The highest-value companions are the harness's own gates the spec already depends on by design — `acceptance-eval`, `outcome-eval`, `verify`, `code-review`, `rollback` (D3/D5/D9/D10) — not external skills. Lower-scored auto-matches (`gof-visitor`, `mobile-navigation-pattern`, `react-progressive-hydration`, `a11y-screen-reader-testing`, `harness-i18n-process`) were dropped as domain-signal noise irrelevant to a routing engine.

---

_Scanned 758 skills. Signals: 9 keywords, 2 stack markers, 7 domains._
