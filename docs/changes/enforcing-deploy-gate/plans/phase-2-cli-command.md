# Plan: Enforcing Deploy Gate — Phase 2 (CLI Command + Wiring + Docs)

**Date:** 2026-08-07 | **Spec:** `docs/changes/enforcing-deploy-gate/proposal.md` (Implementation Order → Phase 2) | **Tasks:** 6 | **Time:** ~26 min | **Integration Tier:** large

## Goal

Ship `harness check-deployment` — a `check-*` CLI command that resolves config, runs the Phase 1 core engine (`detectDeploymentSurface` → `evaluateDeploymentGate` → `deriveExitCode`), prints a readiness report, supports `--json` and `--findings-json`, and `process.exit`s per the four-value exit-code contract (D2). Register it via the barrel generator, regenerate `docs/reference/cli-commands.md`, and add the changeset. No new MCP tool (D1); no skill edits (Phase 3).

## Scope Guards (do NOT do in this plan)

- **Depends on Phase 1 being merged/available.** This plan imports `detectDeploymentSurface`, `evaluateDeploymentGate`, `deriveExitCode`, and the deployment types from `@harness-engineering/core`, and `DeploymentGateConfigSchema` wiring from the CLI config. Do not re-implement the engine here.
- **No new MCP tool (D1).** The surface is a CLI command + the existing engine only. Do not touch the MCP registry or tool count.
- **No default-orchestrator wiring (D6).** Do NOT add `check-deployment` to the default `harness ci check` pipeline. It ships standalone + opt-in.
- **No skill edits, no ADR.** Those are Phase 3.
- **No hand-editing generated files.** `_registry.ts` (barrel) and `docs/reference/cli-commands.md` are regenerated by scripts, never edited by hand.
- **No internal roadmap/PR/issue numbers** in any help text or console output the command emits (shipped-body rule).

## Observable Truths (Acceptance Criteria)

Traces to spec Success Criteria (SC) 1, 2, 3, 4, 5, 6, 7, 8.

1. **SC1.** `harness check-deployment` exists, is registered (appears in `createProgram()` output / `harness --help`), and after docs regen appears in `docs/reference/cli-commands.md`.
2. **SC2/3/4 (block).** _When the engine returns `status: 'blocked'`, the command shall print the hard finding(s) and exit `1`._ Verified over a leaked-secret fixture (`DEPLOY-SEC001`), a no-rollback fixture (`DEPLOY-RB001`, remediation names `harness-rollback`), and an ungated-prod fixture (`DEPLOY-ENV001`).
3. **SC5 (soft-only).** _When the engine returns `status: 'pass'` with only advisories, the command shall list them and exit `0`._
4. **SC6 (abstain).** _When the engine returns `status: 'abstained'`, the command shall print a loud "No deployment configuration detected; deploy gate not applicable (abstained)." message and exit `3`_ — never `0`, never `1`.
5. **SC7 (opt-out).** _When `deployment.enabled === false`, the command shall exit `0` with an explicit opt-out note distinct from the abstention message._
6. **Error path (D2).** _If `harness.config.json` is missing or malformed, the command shall surface the parse error verbatim and exit `2` (`ExitCode.ERROR`)._
7. **`--json` / `--findings-json`.** `--json` prints the full `DeploymentGateResult`; `--findings-json` appends a trailing `formatFindingsContract(count, 'check-deployment')` line. Neither changes the exit code.
8. **Rollback seam wired (D5).** The command sets `rollbackConfigured` on the engine config from `config.rollback != null` so a `rollback` block satisfies `DEPLOY-RB001`.

## Grounding (evidence: file:line)

- **Reference command** — `packages/cli/src/commands/check-arch.ts`: option/action shape (`createCheckArchCommand`, `:490`), `--findings-json` option (`:500`), `resolveOutputMode` (`:439`), JSON short-circuit + `logger.error` on `!result.ok` (`:515-519`), `maybeEmitArchFindings` using `formatFindingsContract(count, 'check-arch')` (`:484-488`), final `process.exit(value.passed ? ExitCode.SUCCESS : ExitCode.VALIDATION_FAILED)` (`:533`). Config resolution: `findConfigFile()` / `loadConfig(configPath)` from `../config/loader` (`:30`, `:299-309`); `cwd = options.cwd ?? path.dirname(configPath)` (`:311`).
- **Exit codes** — `packages/cli/src/utils/errors.ts:4-20`: `SUCCESS=0`, `VALIDATION_FAILED=1`, `ERROR=2`, `ZERO_DENOMINATOR=3`; `CLIError(message, exitCode)` (`:31-46`). A malformed-config `loadConfig` returns `Err` — surface it and `process.exit(ExitCode.ERROR)`.
- **Findings contract** — `formatFindingsContract(findings, check?)` from `@harness-engineering/types` (`packages/types/src/maintenance-findings.ts:52`), imported in check-arch at `:29`.
- **Registry (auto-generated)** — `packages/cli/src/commands/_registry.ts:1` (`// AUTO-GENERATED — do not edit. Run \`pnpm run generate-barrel-exports\``). Commands are imported (`:16` `createCheckArchCommand`) and pushed alphabetically to `commandCreators` (`:122`). A new `check-deployment.ts`exporting`createCheckDeploymentCommand`is picked up by`pnpm generate-barrel-exports`; it lands between `createCheckDesignCommand`and`createCheckDocsCommand` alphabetically.
- **CLI command tests** — `packages/cli/tests/commands/check-arch.test.ts` is the convention for command tests (mkdtemp fixture config, invoke the action).
- **Docs regen** — `pnpm generate-docs` → `scripts/generate-docs.mjs` writes `docs/reference/cli-commands.md` (`:550-551`). Requires a build first (the generator reads built command metadata).
- **Changeset format** — `.changeset/*.md` with front-matter `'@harness-engineering/cli': minor` (single quotes), e.g. `.changeset/adopter-roadmap-hook-installer.md`.
- **Core engine (Phase 1)** — `@harness-engineering/core` exports `detectDeploymentSurface`, `evaluateDeploymentGate`, `deriveExitCode`, and deployment types (added to the core barrel in Phase 1 Task 8). `deriveExitCode` returns `0 | 1 | 2 | 3`; the CLI does NOT re-map — it passes that number to `process.exit`, adding the `2` path only for config-parse failures.

## File Map

- CREATE `packages/cli/src/commands/check-deployment.ts` — `runCheckDeployment` + `createCheckDeploymentCommand`.
- CREATE `packages/cli/tests/commands/check-deployment.test.ts` — action + exit-code + json/findings-json tests.
- MODIFY `packages/cli/src/commands/_registry.ts` — regenerated by `pnpm generate-barrel-exports` (not hand-edited).
- MODIFY `docs/reference/cli-commands.md` — regenerated by `pnpm generate-docs` (not hand-edited).
- CREATE `.changeset/enforcing-deploy-gate-cli.md` — `'@harness-engineering/cli': minor`.

## Skeleton

1. `runCheckDeployment` core (config → engine → result) with TDD (~1 task, ~6 min)
2. Command wiring: exit codes, `--json`, `--findings-json`, report output with TDD (~1 task, ~6 min)
3. Register via generator (~1 task, ~3 min)
4. Regenerate docs (~1 task, ~4 min)
5. Changeset (~1 task, ~2 min)
6. Human-verify checkpoint across all exit codes (~1 task, ~5 min)

**Estimated total:** 6 tasks, ~26 minutes. _Skeleton approved: pending._

## Uncertainties

- [ASSUMPTION] The CLI reads repo files through a small `fs`-backed adapter implementing the Phase 1 `DeploymentFsPort` (sync `existsSync` / `readFileSync` / `readdirSync`), constructed in `runCheckDeployment` from `cwd`. This is the CLI's only IO; the engine stays pure. If Phase 1 shipped a ready-made node adapter in core, import that instead (Task 1 greps for it first).
- [ASSUMPTION] `deriveExitCode` never returns `2`; the CLI raises `ExitCode.ERROR` only on `loadConfig`/`findConfigFile` failure. If Phase 1 chose to fold internal errors into `deriveExitCode`, Task 2 adjusts the mapping accordingly.
- [DEFERRABLE] The exact human-readable report layout (ordering of hard vs soft findings, colorization) mirrors `printArchResult` via `OutputFormatter.formatValidation`; precise wording is finalized during implementation and does not affect exit codes.

---

## Tasks

### Task 1 (TDD): `runCheckDeployment` — resolve config, run engine, return a Result

**Depends on:** none (Phase 1 available) | **Files:** `packages/cli/src/commands/check-deployment.ts`, `packages/cli/tests/commands/check-deployment.test.ts`

1. First: `git grep -n "DeploymentFsPort\|node.*fsPort\|export function nodeFs" packages/core/src/deployment` — if Phase 1 shipped a node fs adapter, import it; otherwise build a tiny inline adapter (sync `fs`).
2. Create `packages/cli/tests/commands/check-deployment.test.ts` (mkdtemp fixture-config convention from `check-arch.test.ts`):
   - **Abstain:** a temp project with only a `harness.config.json` (no CI/CD files, no `deployment` config) → `runCheckDeployment` returns `Ok` with `result.status === 'abstained'`.
   - **Blocked (secret):** a temp project with `.github/workflows/deploy.yml` containing `AWS: "AKIAIOSFODNN7EXAMPLE"` and `deployment: { enabled: true }` → `result.status === 'blocked'`, a `DEPLOY-SEC001` finding.
   - **Disabled:** `deployment: { enabled: false }` → `result.status === 'disabled'`.
   - **Error:** a malformed `harness.config.json` → `runCheckDeployment` returns `Err` with `error.exitCode === ExitCode.ERROR`.
3. Run: `npx vitest run packages/cli/tests/commands/check-deployment.test.ts` — observe failures.
4. Create `packages/cli/src/commands/check-deployment.ts` with `runCheckDeployment(options): Promise<Result<DeploymentGateResult, CLIError>>`:
   - Resolve config: `options.configPath ? Ok(...) : findConfigFile()`; then `loadConfig(configPath)`; on either failure return the `Err` (already carries `ExitCode.ERROR`) — mirror `check-arch.ts:299-309`.
   - `cwd = options.cwd ?? path.dirname(configPath)`.
   - Build the `DeploymentFsPort` adapter over `cwd`.
   - `const surface = detectDeploymentSurface(cwd, fsPort);`
   - `const gateConfig = { ...(config.deployment ?? {}), rollbackConfigured: config.rollback != null };` (wires the D5 seam).
   - `const result = evaluateDeploymentGate(surface, gateConfig);` return `Ok(result)`.
5. Run: `npx vitest run packages/cli/tests/commands/check-deployment.test.ts` — expect the Task 1 tests pass.
6. Run: `harness validate`
7. Commit: `feat(cli): runCheckDeployment resolves config and runs the deploy gate engine`

### Task 2 (TDD): Command wiring — exit codes, `--json`, `--findings-json`, report

**Depends on:** Task 1 | **Files:** `packages/cli/src/commands/check-deployment.ts`, `packages/cli/tests/commands/check-deployment.test.ts`

1. Append tests to `check-deployment.test.ts` that drive `createCheckDeploymentCommand()` and assert `process.exit` codes (spy on `process.exit`, mirror check-arch test convention):
   - abstain fixture → exit `3` and a stdout/stderr line containing `abstained`.
   - blocked fixture → exit `1`.
   - soft-only fixture (deploy target + rollback signal + gated prod, missing a stage) → exit `0` with the advisory listed.
   - disabled fixture → exit `0` with an opt-out note (distinct string from the abstain message).
   - malformed config → exit `2`.
   - `--findings-json` → last stdout line parses as JSON with a numeric `findings` and `check === 'check-deployment'`.
   - `--json` → stdout is the full `DeploymentGateResult` (parses, has `status`).
2. Run: `npx vitest run packages/cli/tests/commands/check-deployment.test.ts` — observe the new failures.
3. Extend `check-deployment.ts` (mirror `check-arch.ts:439-537`):
   - `resolveOutputMode(globalOpts)` helper (or reuse the shared one).
   - `buildDeploymentIssues(result)` → `{ file?, message }[]` from `hardViolations` then `softViolations` (hard first), each message prefixed with the code and severity.
   - `printDeploymentResult(result, mode, formatter)` — JSON mode prints `JSON.stringify(result, null, 2)`; otherwise print a readiness report via `formatter.formatValidation({ valid: status === 'pass' || status === 'disabled', issues })`, and for `abstained`/`disabled` print the loud message.
   - `maybeEmitDeploymentFindings(findingsJson, result)` → `console.log(formatFindingsContract(buildDeploymentIssues(result).length, 'check-deployment'))`.
   - `createCheckDeploymentCommand()`: `new Command('check-deployment').description('Verify deployment readiness and gate a deploy on hard violations').option('--findings-json', 'Emit findings contract as a trailing JSON line')`. In the action: call `runCheckDeployment`; on `!result.ok` print error + `process.exit(result.error.exitCode)`; else print the report, maybe emit findings, and `process.exit(deriveExitCode(result.value))`.
   - **No internal issue/PR numbers** in any description or message string.
4. Run: `npx vitest run packages/cli/tests/commands/check-deployment.test.ts` — expect all pass.
5. Run: `harness validate`
6. Commit: `feat(cli): check-deployment command — exit-code contract, --json/--findings-json`

### Task 3: Register the command via the barrel generator

**Depends on:** Task 2 | **Files:** `packages/cli/src/commands/_registry.ts` (generated) | **Category:** integration

1. Run: `pnpm generate-barrel-exports`.
2. Verify the generated `_registry.ts` now imports `createCheckDeploymentCommand` from `./check-deployment` and lists it in `commandCreators` alphabetically (between `createCheckDesignCommand` and `createCheckDocsCommand`). Do NOT hand-edit; if it did not appear, confirm the command file exports `createCheckDeploymentCommand` with the exact name and re-run.
3. Build the CLI: `pnpm --filter @harness-engineering/cli build`.
4. Smoke test: `node packages/cli/dist/bin/harness.js check-deployment --help` prints the description (or `harness check-deployment --help` if a dev alias is available).
5. Run: `harness validate` and `harness check-deps` — expect pass (barrel in sync).
6. Commit: `feat(cli): register check-deployment command`

### Task 4: Regenerate the CLI command reference docs

**Depends on:** Task 3 | **Files:** `docs/reference/cli-commands.md` (generated) | **Category:** integration

1. Ensure a fresh build (Task 3 built the CLI; rebuild if anything changed): `pnpm --filter @harness-engineering/cli build`.
2. Run: `pnpm generate-docs`.
3. Verify `docs/reference/cli-commands.md` now contains a `check-deployment` section with its description and options (`--findings-json`, `--json`). Do NOT hand-edit — if it is missing, confirm the build succeeded and the command registered (Task 3), then re-run.
4. Confirm no internal roadmap/PR/issue numbers leaked into the generated command help (grep the new section for `#\d`).
5. Run: `harness validate`
6. Commit: `docs(cli): regenerate cli-commands reference with check-deployment`

### Task 5: Add the changeset

**Depends on:** Task 2 | **Files:** `.changeset/enforcing-deploy-gate-cli.md` | **Category:** integration

1. Create `.changeset/enforcing-deploy-gate-cli.md`:

   ```md
   ---
   '@harness-engineering/cli': minor
   '@harness-engineering/core': minor
   ---

   Add `harness check-deployment` — an enforcing pre/post-deploy gate backed by a
   pure `packages/core/src/deployment` engine. It verifies deployment readiness and
   exits non-zero on unambiguous, incident-causing violations so CI can gate a deploy:
   a hardcoded secret in a pipeline or committed env file (`DEPLOY-SEC001`,
   non-waivable), a deploy target with no rollback path wired (`DEPLOY-RB001`), and a
   direct-to-production deploy with no promotion/approval gate (`DEPLOY-ENV001`).
   Maturity gaps (missing stages, weak env separation, no health check, pipeline
   smells) are surfaced as non-blocking advisories. On a repo with no deployment
   configuration the gate abstains loudly (exit 3, never a false green); `enabled:
   false` opts out explicitly (exit 0). The rollback requirement is satisfied by a
   `rollback` config block, a revert/rollback workflow or script, or a documented
   runbook, tying the pre-ship gate to the post-ship rollback circuit breaker. The
   gate is standalone and opt-in via `deployment.enabled` — it is not added to the
   default `ci check`.

   The `@harness-engineering/core` bump ships the new `deployment` engine module
   (detect + evaluate + exit-code) reused by the command.
   ```

   > Do NOT include internal roadmap/PR/issue numbers in the changeset body (it ships in the published changelog). The spec/plan/ADR may reference them; the changeset may not.

2. Run: `harness validate`
3. Commit: `chore(changeset): enforcing deploy gate CLI command`

### Task 6: `[checkpoint:human-verify]` — end-to-end exit-code contract over fixtures

**Depends on:** Tasks 3, 4, 5 | **Files:** none (verification only) | **Category:** integration

1. Build: `pnpm --filter @harness-engineering/cli build`.
2. Create four throwaway fixture dirs under the scratchpad (not committed) and run `harness check-deployment -c <fixture>/harness.config.json` against each, asserting the exit code with `echo $?`:
   - **Secret leak** (`.github/workflows/deploy.yml` with a hardcoded key) → exit `1`, `DEPLOY-SEC001` printed.
   - **No rollback** (prod deploy target, no rollback signal, no `rollback` config) → exit `1`, `DEPLOY-RB001` whose remediation names `harness-rollback`.
   - **Ungated prod** (push-to-main deploy, no approval/environment/staging) → exit `1`, `DEPLOY-ENV001`.
   - **Soft-only** (deploy target + rollback + gated prod, missing a stage) → exit `0`, advisory listed.
   - **No config** (empty repo) → exit `3`, loud abstention message.
   - **Opt-out** (`deployment.enabled: false`) → exit `0`, opt-out note.
   - **`DEPLOY-SEC001: off` override** on the secret-leak fixture → still exit `1` (non-waivable, SC8).
3. Confirm `--json` emits the full result and `--findings-json` appends the contract line, and neither alters the exit code.
4. Run: `harness validate` and `harness check-deps`.
5. `[checkpoint:human-verify]` — Present the per-fixture exit codes (1/1/1/0/3/0/1) and the registered-command + docs-section evidence. Confirm the command is NOT in the default `ci check` pipeline (D6). Wait for confirmation before considering Phase 2 complete.
6. Commit (if any format/lint churn): `chore(cli): finalize check-deployment wiring`

---

## Sequencing

- Task 1 → Task 2 is strict sequential TDD on `check-deployment.ts` (shared file).
- Task 3 (register) depends on Task 2; Task 4 (docs) depends on Task 3 (needs the registered, built command).
- Task 5 (changeset) depends only on Task 2 and may run in parallel with Tasks 3-4.
- Task 6 (checkpoint) depends on Tasks 3, 4, 5.

## Traceability

| Observable truth (SC)        | Delivered by   |
| ---------------------------- | -------------- |
| SC1 exists + registered      | Tasks 3, 4     |
| SC2/3/4 block → exit 1       | Tasks 2, 6     |
| SC5 soft-only → exit 0       | Tasks 2, 6     |
| SC6 abstain → exit 3         | Tasks 2, 6     |
| SC7 opt-out → exit 0         | Tasks 2, 6     |
| Error path → exit 2 (D2)     | Tasks 1, 2     |
| `--json` / `--findings-json` | Task 2         |
| Rollback seam (D5)           | Task 1, Task 6 |
| Changeset present            | Task 5         |

## Concerns

- Docs regen (Task 4) requires a successful build; if the monorepo's `generate-docs` reads built `dist` metadata, the CLI must be built first or the command will be missing from the reference. The task builds before regenerating.
- The CLI must set `rollbackConfigured` from `config.rollback != null` (Task 1) or `DEPLOY-RB001` will false-fire on repos that wired the `harness-rollback` circuit breaker — this is the D5 seam and is easy to forget. Task 6's no-rollback fixture deliberately omits `rollback` to prove the positive; add a variant with a `rollback` block to prove it is satisfied.
- Keep all command help/output free of internal issue/PR numbers (shipped-body rule). Task 2 and Task 4 both check.
