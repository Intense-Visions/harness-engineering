{
  "name": "harness-engineering",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Comprehensive toolkit for agent-first development",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Intense-Visions/harness-engineering.git"
  },
  "license": "MIT",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "test:ci": "turbo run test:coverage",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,md,json}\"",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && node scripts/clean.mjs node_modules",
    "bench": "turbo run bench",
    "test:platform-parity": "vitest run tests/platform-parity.test.ts",
    "orchestrator": "node packages/cli/dist/bin/harness.js orchestrator run",
    "orchestrator:dev": "pnpm --filter @harness-engineering/cli build && HARNESS_PROJECT_PATH=$PWD concurrently --names orch,dash --prefix-colors blue,green \"node packages/cli/dist/bin/harness.js orchestrator run --headless\" \"HARNESS_PROJECT_PATH=$PWD pnpm --filter @harness-engineering/dashboard dev\"",
    "dashboard:dev": "HARNESS_PROJECT_PATH=$PWD pnpm --filter @harness-engineering/dashboard dev",
    "dashboard:build": "pnpm --filter @harness-engineering/dashboard build",
    "docs:dev": "pnpm --filter docs dev",
    "docs:build": "pnpm --filter docs build",
    "docs:preview": "pnpm --filter docs preview",
    "generate-docs": "node scripts/generate-docs.mjs",
    "generate-barrel-exports": "node scripts/generate-barrel-exports.mjs",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm build && changeset publish",
    "prepare": "husky"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "@eslint/js": "^10.0.1",
    "@harness-engineering/eslint-plugin": "workspace:^",
    "@typescript-eslint/eslint-plugin": "^8.58.0",
    "@typescript-eslint/parser": "^8.58.0",
    "concurrently": "^9.1.0",
    "eslint": "^10.1.0",
    "eslint-config-prettier": "^10.1.8",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.1",
    "tsup": "^8.5.1",
    "turbo": "^2.9.5",
    "typescript": "^5.9.3",
    "vitepress": "^1.6.4",
    "vitest": "^4.1.2",
    "yaml": "^2.8.3"
  },
  "lint-staged": {
    "packages/*/src/**/*.{ts,tsx}": [
      "eslint --max-warnings 0 --no-warn-ignored",
      "prettier --write"
    ],
    "*.{ts,tsx,md,json}": "prettier --write",
    "package.json": "scripts/sync-lockfile.sh"
  },
  "packageManager": "pnpm@8.15.4",
  "pnpm": {
    "autoInstallPeers": true,
    "strictPeerDependencies": false,
    "overrides": {
      "hono": ">=4.12.12",
      "@hono/node-server": ">=1.19.13"
    },
    "auditExceptions": {
      "GHSA-67mh-4wv8-2f99": "esbuild dev-server CORS — dev-only, no network exposure. Blocked by vitepress ^5 pin.",
      "GHSA-4w7w-66w2-5vf9": "vite .map path traversal — dev-only, no --host usage. Blocked by vitepress ^5 pin."
    }
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=8.0.0"
  }
}
