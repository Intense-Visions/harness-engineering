import { Command } from 'commander';

/** Placeholder — replaced in Task 3 */
export function createListIntegrationsCommand(): Command {
  return new Command('list').description('Show all MCP integrations with status');
}
