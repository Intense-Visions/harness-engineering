# Models

The `CompanyKnowledge` dataclass exposes `dashboard_url` and
`dashboard_token_env`. Call `refresh` to reload it.

The default timeout is `TIMEOUT_SECONDS` seconds.

`SuiteType` covers `E2E_UI` and `PERFORMANCE` runs.

The `ghost_symbol` helper was removed and no longer exists.
