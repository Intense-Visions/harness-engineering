ESLint rule 'no-empty-describe' has been successfully implemented and verified:

1. Fixed duplicate key error in src/index.ts where the rule was defined twice
2. Rule implementation correctly detects empty describe blocks with no statements
3. Rule does NOT trigger for describes containing statements
4. Unit tests exist and cover valid/invalid cases
5. All tests pass (240 tests passed)
6. Build completes successfully
7. Rule is properly registered in plugin configuration
8. Rule follows existing code patterns in the project

The implementation satisfies all requirements:
- Flags empty describe blocks that are leftover scaffolding
- Does not report describes containing statements  
- Has proper RuleTester unit tests
- Is registered in the plugin
- Updates plugin integration rule-count
- Passes pnpm --filter @harness-engineering/eslint-plugin test