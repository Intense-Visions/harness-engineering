/**
 * Native recommender — public barrel (Phase 6).
 *
 * Exposes the `createNativeRecommender` seam the scheduler binds each tick to
 * turn the operator's explicit candidate set into a hardware-aware ranking.
 */

export * from './native.js';
