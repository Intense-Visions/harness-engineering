/**
 * Background scheduler — public barrel (Phase 6).
 *
 * Exposes the timer-free `runRefreshTick` pipeline and the overlap-guarded
 * `RefreshScheduler` the orchestrator arms once per instance.
 */

export * from './refresh.js';
