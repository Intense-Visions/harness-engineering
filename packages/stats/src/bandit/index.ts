/**
 * Explore/exploit bandit — public surface of the instrument.
 *
 * Phase 1 placeholder. The ledger (`ledger.ts`), arm model (`arm-model.ts`),
 * policies (`policy.ts`), and default utilities (`utility.ts`) land in Phase 2
 * (docs/changes/stats-explore-exploit/proposal.md, "Implementation order").
 */
export {};
