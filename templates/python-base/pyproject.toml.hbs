[project]
name = "{{projectName}}"
version = "0.1.0"
description = ""
requires-python = ">={{pythonMinVersion}}"
dependencies = []

[project.optional-dependencies]
dev = [
  "pytest>=7.0",
  "ruff>=0.4.0",
]

[tool.ruff]
line-length = 88

[tool.pytest.ini_options]
testpaths = ["tests"]
