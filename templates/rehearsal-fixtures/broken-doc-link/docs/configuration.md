# Configuration

<!-- FIXTURE (deliberately broken): this is the ONLY sibling page that exists. -->
<!-- getting-started.md links here (resolves) and to api-reference.md (does not). -->

Set the following before your first run:

- `APP_ENV` — `development` or `production`.
- `APP_PORT` — the port to listen on.

Back to [Getting started](./getting-started.md).
